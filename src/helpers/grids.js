// export const newGrid = (props) => {
//   const rows = [];
//   for (let i = 0; i < props.size; i++) {
//     rows.push(Array.from(Array(props.size), () => 0));
//   }
//   return rows;
// };

// export const randomGrid = (props) => {
//   const rows = [];
//   for (let i = 0; i < props.size; i++) {
//     rows.push(
//       Array.from(Array(props.size), () => (Math.random() > 0.6 ? 1 : 0))
//     );
//   }
//   return rows;
// };
